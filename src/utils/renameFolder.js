// const fs = require("fs");
// const path = require("path");

// function renameFolder(folderName, newFolderName) {
//   console.log(folderName, newFolderName);
//   fs.rename(
//     path.join(__dirname, "..", "..", "public", "images", folderName),
//     newFolderName,
//     (err) => {
//       if (err) {
//         console.error(err);
//         return;
//       }
//       console.log(
//         `Folder "${folderName}" has been renamed to "${newFolderName}".`
//       );
//     }
//   );
// }

// module.exports = renameFolder;
